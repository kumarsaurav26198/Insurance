import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ImageTextCustomButton = () => {
    return (
        <View>
            <Text>ImageTextCustomButton</Text>
        </View>
    );
};

export default ImageTextCustomButton;

const styles = StyleSheet.create({});